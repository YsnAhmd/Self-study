let value;
// let val1 = Number(prompt('Enter a number: '));
// let val2 = Number(prompt('Enter a number: '));
let val1 = 10
let val2 = 10

// Simple math
value = val1 + val2;
value = val1 - val2
value = val1 * val2
value = val1 / val2
value = val1 % val2

// Math object
value = Math.E //gives value of eulers number/E value
value = Math.PI //Gives value of PI
value = Math.sqrt(4) //Used for squre root
value = Math.pow(2, 3) //used for giving power
value = Math.round(2.5) //Set the value to a immediate full nomber
value = Math.floor(2.5) //set the value to lowest full number 
value = Math.ceil(2.5) // set the value to height full number
value = Math.abs(-2.5) // used for finding out absulate value
value = Math.random() // Gives random number from 0-1 by default

// Output
console.log(value);